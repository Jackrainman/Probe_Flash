# STM32机器人固件 Debug Checklist

> 自动生成于 2026-05-12
> 分析范围：从仓库创建到最新提交 (6218dd5) 的所有历史问题

---

## 一、历史Bug修复记录 (按类型分类)

### 1. 时序/初始化问题 [5个]

| 提交ID | 提交信息 | 根本原因 | 影响模块 |
|--------|----------|----------|----------|
| `06e013f` | fix: defer Damiao startup to task | 电机在初始化阶段使能，未等待上电稳定 | catch_rod, chassis |
| `f5d48d3` | fix delay bug | 主控和电机同时上电，电机需要稳定时间 | 系统启动 |
| `f67041a` | finding can callback problem | FDCAN2中断配置错误，GPIO引脚配置问题 | CAN通信 |
| `81a8822` | fix systemview buffer | SystemView缓冲区过小，任务数量限制不足 | 调试工具 |
| `e338d1a` | chassis auto point run bug fix | USART3 DMA中断未配置 | 串口通信 |

**检查清单：**
- [ ] 所有电机使能是否延迟到任务启动后执行？
- [ ] CAN中断配置是否正确？
- [ ] 调试缓冲区大小是否充足？(当前32KB)
- [ ] 串口DMA中断是否完整配置？

### 2. 状态机/逻辑错误 [5个]

| 提交ID | 提交信息 | 根本原因 | 影响模块 |
|--------|----------|----------|----------|
| `7c59481` | report bug fix | yaw_source未上报，默认源设置错误 | msg_process |
| `770c079` / `ceaf579` | bug fix | 按键映射冲突 | main_ctrl |
| `e0fa62b` | chassis bug fixes | 数组越界，单位转换错误 | chassis |
| `e49169d` | run yaw bug fix | 速度反馈错误，上报设定值而非实际值 | chassis |
| `50dbc67` | fix chassis bug | 参数命名混乱，宏定义不一致 | chassis |

**检查清单：**
- [ ] 所有状态字段是否完整上报？
- [ ] 按键编号是否有冲突？(范围1-50)
- [ ] 数组访问是否有边界检查？
- [ ] 单位转换是否正确？(deg/s vs rad/s)

### 3. 内存/缓冲区问题 [3个]

| 提交ID | 提交信息 | 根本原因 | 影响模块 |
|--------|----------|----------|----------|
| `703ef83` | bug fix | FreeRTOS堆不足(16KB→32KB) | FreeRTOS配置 |
| `a58e8e9` | auto_path bug fix | A*路径数组越界 | chassis/path |
| `f5d48d3` | fix delay bug | ISR队列溢出，FIFO未清空 | CAN驱动 |

**检查清单：**
- [ ] FreeRTOS堆大小是否充足？(当前32KB，建议监控使用率)
- [ ] A*路径数组大小是否足够？(astar_raw_ids[100])
- [ ] CAN回调是否有队列判空保护？

### 4. 坐标系/数学计算问题 [3个]

| 提交ID | 提交信息 | 根本原因 | 影响模块 |
|--------|----------|----------|----------|
| `f1b6ad9` | fix radar offset | 雷达偏移未随车身旋转补偿 | chassis定位 |
| `c5cc26d` | fix first point problem | A*起点可能位于不可通行区域 | A*路径规划 |
| `e0fa62b` | chassis bug fixes | 单位转换位置错误 | chassis |

**检查清单：**
- [ ] 雷达偏移补偿是否考虑车身旋转？
- [ ] A*起点是否有效？是否实现8邻域搜索？
- [ ] 单位转换是否在正确层级进行？

### 5. 机械/硬件适配问题 [4个]

| 提交ID | 提交信息 | 根本原因 | 影响模块 |
|--------|----------|----------|----------|
| `eca71e6` | catch servo deg fix | 舵机角度标定错误，夹持不牢 | catch_rod |
| `57475bc` | fix extend position | 步进电机原点漂移 | lift_ctrl |
| `bec1f46` | fix extend | 伸缩方向逻辑错误，左右臂方向相反 | lift_ctrl |
| `06e013f` | lift/chassis target tuning | 机械点位需要适配实际场地 | 点位配置 |

**检查清单：**
- [ ] 舵机角度是否需要重新标定？
- [ ] 步进电机上电是否等待位置同步？
- [ ] 左右臂伸缩方向是否正确？
- [ ] 跑点点位坐标是否匹配实际场地？

### 6. 代码规范/类型问题 [2个]

| 提交ID | 提交信息 | 根本原因 | 影响模块 |
|--------|----------|----------|----------|
| `fa397fd` | bug fix | 全局变量命名冲突 | 全局命名空间 |
| `770c079` / `ceaf579` | bug fix | 布尔类型误用uint8_t | 类型系统 |

---

## 二、代码质量隐患 (当前代码中)

### 1. 数组越界风险 [高风险]

| 位置 | 问题描述 | 建议修复 |
|------|----------|----------|
| `remote_ctrl.c:22-28` | `event`参数没有范围检查，可能越界 | 添加 `event < REMOTE_KEY_EVENT_NUM` 检查 |
| `chassis.c:438-444` | 虽然有检查但POS_NUM是运行时计算 | 使用编译时常量定义 |
| `main_ctrl.c:82-88` | `key`参数可能大于REMOTE_KEY_NUM | 添加输入参数校验 |

**检查清单：**
- [ ] 所有数组访问是否有边界检查？
- [ ] 动态计算的数组大小是否一致？

### 2. 空指针风险 [高风险]

| 位置 | 问题描述 | 建议修复 |
|------|----------|----------|
| `infrared.c:229-231` | `irda_get_state`未检查NULL | 添加 `if (state == NULL) return;` |
| `msg_process.c:154-155` | 偏移计算可能出错 | 添加除零检查 |
| `steering_wheel.c:250-252` | `wheel_speed[i]`可能为0 | 添加非零检查 |

**检查清单：**
- [ ] 所有指针参数是否检查NULL？
- [ ] 除法运算是否有除零保护？

### 3. 资源泄漏风险 [高风险]

| 位置 | 问题描述 | 建议修复 |
|------|----------|----------|
| `smd.c:105` | malloc未检查返回值 | 添加NULL检查 |
| `unitree_motor.c:332,362` | malloc未检查返回值 | 添加NULL检查 |
| `usart_ex.c:185-186,205` | malloc未检查返回值 | 添加NULL检查 |
| `chassis.c:1152-1178` | configASSERT在return之后 | 调整断言位置 |

**检查清单：**
- [ ] 所有malloc/calloc是否检查返回值？
- [ ] 任务/队列创建失败处理是否正确？

### 4. 竞态条件 [高风险]

| 位置 | 问题描述 | 建议修复 |
|------|----------|----------|
| `lift_ctrl.c:238-253` | 临界区内浮点运算过长 | 移出临界区，只保护标志位访问 |
| `chassis.c:621-629` | 临界区内浮点运算 | 缩短临界区 |
| `msg_process.c:157-162` | `nuc_pose_init`在临界区外计算 | 移入临界区或原子访问 |
| `chassis.c:688-690` | 直接修改g_nuc_pos_data无保护 | 添加临界区保护 |

**检查清单：**
- [ ] 临界区内是否只有必要操作？
- [ ] 全局状态修改是否有同步保护？

### 5. 死代码

| 位置 | 问题描述 | 建议 |
|------|----------|------|
| `lift_ctrl.c:309-333` | `#if 0`包裹的增量升降接口 | 移除或归档到历史文档 |
| `chassis.c:1193-1310` | `#if 0`包裹的Theta*算法 | 移除或归档 |
| `catch_rod.c:758-800` | `#if 0`包裹的历史平衡逻辑 | 移除或归档 |
| `remote_ctrl.c:20,133-142` | 注释掉的point_callback代码 | 清理头文件声明 |

**检查清单：**
- [ ] 所有`#if 0`代码是否需要保留？
- [ ] 头文件声明是否有对应实现？

### 6. 硬编码魔法数字

| 位置 | 问题描述 | 建议 |
|------|----------|------|
| `main_ctrl.c:178-201` | 硬编码位置值20.5f, 0.0f等 | 使用具名常量定义 |
| `chassis.c:148` | 硬编码点位坐标 | 使用配置文件或宏定义 |
| `catch_rod.c:32-49` | 硬编码舵机角度 | 使用宏定义说明含义 |

**检查清单：**
- [ ] 所有魔法数字是否有说明？
- [ ] 点位坐标是否有来源文档？

---

## 三、架构设计问题

### 1. 层间依赖违规

| 文件 | 违规内容 | 违反原则 |
|------|----------|----------|
| `infrared.c:13` | `#define IRDA_UART_HANDLE &huart3` | Application层不应直接访问HAL句柄 |
| `lift_ctrl.c:20-25` | `HAL_GPIO_WritePin(PUMP_*)` | Application层不应直接操作GPIO |
| `msg_process.c:11-12` | 直接定义UART句柄 | 应通过BSP层抽象 |
| `catch_rod.c:51-52` | 直接使用htim1/htim3 | 应通过BSP层抽象 |

**检查清单：**
- [ ] Application层是否只通过bsp.h访问硬件？
- [ ] 所有HAL句柄访问是否封装到BSP层？

### 2. 全局变量滥用

| 变量名 | 定义位置 | 使用位置 | 风险等级 |
|--------|----------|----------|----------|
| `g_remote_ctrl_data` | remote_ctrl.c | catch_rod.c, chassis.c, main_ctrl.c | 中 |
| `g_nuc_pos_data` | msg_process.c | chassis.c | 高 |
| `g_nuc_ctrl_data` | msg_process.c | chassis.c | 高 |
| `g_nuc_rx_mode` | msg_process.c | chassis.c, msg_process.c | 中 |

**检查清单：**
- [ ] 全局变量是否可以通过接口函数替代？
- [ ] 跨模块全局变量访问是否有同步保护？

### 3. 接口命名不一致

| 模块 | 状态获取接口 | 命名问题 |
|------|-------------|----------|
| chassis | `chassis_get_state()` | 命名规范 |
| lift | `lift_ctrl_get_state()` | 命名不一致 |
| infrared | `irda_get_state()` | 前缀不一致 |
| catch | `catch_get_state()` | 命名规范 |

**建议统一为：** `xxx_get_state(xxx_state_t*)`

### 4. 头文件组织问题

- `includes.h` 职责过重，包含所有模块定义
- 应拆分为各模块独立头文件
- `trajectory_plan.h` (Utils层) 不应依赖 `cubemx.h`

---

## 四、硬件配置问题

### 1. 引脚冲突 [严重]

| 引脚 | 冲突功能1 | 冲突功能2 | 影响模块 |
|------|-----------|-----------|----------|
| PA8 | FDCAN3_RX | BEEP | CAN通信/蜂鸣器 |
| PA2 | TIM2_CH3 | PCA9685_SDA | PWM/舵机驱动 |

**检查清单：**
- [ ] PA8引脚冲突是否已解决？
- [ ] PA2引脚冲突是否已解决？
- [ ] PCA9685是否使用硬件I2C？

### 2. PWM初始配置问题

| 问题 | 位置 | 影响 |
|------|------|------|
| TIM1_CH3/CH4初始脉宽为0 | `tim.c:89` | 舵机初始化可能无输出 |
| TIM2周期与蜂鸣器不匹配 | `tim.c` | 音乐播放功能受限 |

**检查清单：**
- [ ] TIM1所有通道是否有默认脉宽配置？
- [ ] TIM2周期是否支持蜂鸣器频率需求？

### 3. DMA配置问题

| 问题 | 描述 | 建议 |
|------|------|------|
| 所有DMA优先级相同 | 可能影响实时性 | 根据实时性需求分级 |
| UART4波特率极高 | 4Mbps需要高质量布线 | 检查PCB质量 |

**检查清单：**
- [ ] DMA优先级是否根据实时性需求配置？
- [ ] 高速串口布线是否符合要求？

### 4. CAN总线配置

| 问题 | 描述 | 建议 |
|------|------|------|
| FDCAN3引脚冲突 | PA8与蜂鸣器冲突 | 重映射引脚 |
| CAN波特率配置 | 1Mbps | 验证终端电阻 |

---

## 五、测试验证清单

### 1. 功能测试

- [ ] 上电自检流程正常
- [ ] 遥控器按键响应
- [ ] 底盘手动控制
- [ ] 底盘自动跑点
- [ ] 抬升装置升降
- [ ] 夹爪舵机控制
- [ ] 红外通信
- [ ] NUC通信
- [ ] 蜂鸣器提示音

### 2. 边界条件测试

- [ ] 数组越界访问测试
- [ ] 空指针保护测试
- [ ] 除零保护测试
- [ ] 队列满/空处理
- [ ] 任务栈溢出检测
- [ ] 堆内存使用监控

### 3. 压力测试

- [ ] 长时间运行稳定性
- [ ] 连续跑点测试
- [ ] CAN总线负载测试
- [ ] 串口通信压力测试
- [ ] FreeRTOS任务调度

### 4. 硬件兼容性测试

- [ ] 所有电机正常响应
- [ ] 所有舵机正常响应
- [ ] 传感器数据采集
- [ ] 通信接口稳定性
- [ ] 电源电压稳定性

---

## 六、持续监控指标

### 1. 运行时指标

```c
// FreeRTOS运行时统计
configUSE_TRACE_FACILITY = 1
configGENERATE_RUN_TIME_STATS = 1

// 监控项：
- 任务栈使用率
- 堆内存使用率
- 任务切换频率
- 中断响应时间
```

### 2. 通信质量指标

```c
// CAN总线
- 错误帧计数
- 总线负载率
- 消息丢失率

// 串口通信
- 帧错误率
- 丢包率
- 重传次数
```

### 3. 定位精度指标

```c
// 底盘定位
- 点位到达精度 (mm)
- 角度跟踪精度 (deg)
- 路径跟踪误差 (mm)
```

---

## 七、修复优先级建议

### P0 - 立即修复 (阻止功能/安全风险)

1. PA8/PA2引脚冲突问题
2. 数组越界风险 (remote_ctrl.c event参数)
3. 空指针解引用风险
4. FreeRTOS堆不足 (已修复，需持续监控)

### P1 - 尽快修复 (潜在故障风险)

1. 竞态条件 (lift_ctrl.c, chassis.c)
2. malloc返回值未检查
3. 临界区内浮点运算过长
4. 全局变量同步保护

### P2 - 计划修复 (代码质量问题)

1. 死代码清理
2. 硬编码魔法数字
3. 接口命名统一
4. 层间依赖规范

### P3 - 优化建议 (可维护性)

1. includes.h拆分
2. 文档完善
3. 单元测试覆盖
4. 代码格式化

---

## 八、历史Bug趋势分析

### Bug类型分布

```
时序/初始化问题    ████████░░░░░░░░░░░░  23% (5个)
状态机/逻辑错误    ████████░░░░░░░░░░░░  23% (5个)
内存/缓冲区问题    █████░░░░░░░░░░░░░░░  14% (3个)
坐标系/数学计算    █████░░░░░░░░░░░░░░░  14% (3个)
机械/硬件适配      ██████░░░░░░░░░░░░░░  18% (4个)
代码规范/类型      ████░░░░░░░░░░░░░░░░   9% (2个)
```

### 关键教训

1. **时序问题是嵌入式固件的首要问题** - 电机初始化、外设启动顺序必须仔细验证
2. **单位转换错误高发** - deg/s与rad/s混用多次导致bug
3. **数组越界需要防御性编程** - 所有数组访问添加边界检查
4. **全局状态需要同步保护** - 跨任务/ISR访问必须使用临界区或互斥量
5. **硬件引脚冲突必须提前验证** - CubeMX配置需与实际电路图一致

---

## 九、附录：关键代码片段

### 1. 边界检查示例

```c
// 推荐写法
void chassis_set_point_run(uint8_t index) {
    if (index >= POS_NUM) {
        log_message(LOG_WARNING, "Index %d out of range [0,%d)", index, POS_NUM);
        return;
    }
    // ...
}
```

### 2. 临界区使用规范

```c
// 不推荐：临界区内浮点运算
void update_trajectory(void) {
    taskENTER_CRITICAL();
    t_trajectory_init(&traj, ...);  // 浮点运算过长
    taskEXIT_CRITICAL();
}

// 推荐：只保护必要操作
void update_trajectory(void) {
    taskENTER_CRITICAL();
    bool changed = handle.is_pos_changed;
    handle.is_pos_changed = false;
    taskEXIT_CRITICAL();
    
    if (changed) {
        t_trajectory_init(&traj, ...);  // 在临界区外执行
    }
}
```

### 3. 空指针检查示例

```c
// 推荐写法
void irda_get_state(irda_state_t *state) {
    if (state == NULL) {
        return;
    }
    taskENTER_CRITICAL();
    *state = irda_handle.state;
    taskEXIT_CRITICAL();
}
```

---

*本检查清单由Claude Code自动生成，基于对仓库提交历史和当前代码状态的全面分析。*
